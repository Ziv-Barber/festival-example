// @flow

export type PartnerBoxPropsType = {
  /** The name of the brand. */
  name: string,
  /** The logo image URL of the brand. */
  logo: string,
  /** Class name to use for the partner box. */
  className?: string
}
