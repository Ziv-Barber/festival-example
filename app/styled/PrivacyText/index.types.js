// @flow

import * as React from 'react' // eslint-disable-line no-unused-vars

export type PrivacyTextPropsType = {
  /** Class for the root element. */
  className?: string,
  children?: React.Node
}
